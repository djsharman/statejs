import AbstractDoorState from "./<%=abstractName%>";
<% for (let transition in allTransitions) {  %>import <%= transitions[transition] %> from "./<%= transitions[transition] %>";
<% }; %>
//>>>> don't change the CUSTOM CODE comments, if you do generation will overwrite your code >>>>
//###START_CUSTOMCODE1
<%= section1 %>
//###END_CUSTOMCODE1
class <%= className %> extends <%=abstractName%> {
//>>>> don't change the CUSTOM CODE comments, if you do generation will overwrite your code >>>>
//###START_CUSTOMCODE2
<%= section2 %>
//###END_CUSTOMCODE2

  constructor() {
    super();
  }

  <% if (onEnterState) { %>
  onEnterState() {
    
  }
  <% }
  if (onExitState) { %>
  onExitState() {
    
  }
  <% } 
  for (let transition in transitions) {  %>
  <%= transition %>() {
    return new <%= transitions[transition] %>();
  }
  <% }; %>
}
export default OpenDoorState;
